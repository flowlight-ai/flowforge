# Root pyproject.toml — 指向归档（Stub）
#
# FlowForge 主栈已是 TypeScript（pnpm 管 TS）。Python 旧版运行时代码已随阶段 11 S11.2
# （2026-09-16）归档至 `python/legacy/`，本文件仅作为 Python 侧指引。
# 如需使用旧 Python 运行时（仅归档/调试，不用作主线开发）：
#   pip install -e python/legacy       （可选，安装到当前环境）
#   python -m pytest python/legacy     （pytest 用例已随归档迁至 python/legacy/tests）
#   python -m flowforge                （Python 启动入口，会打印 DEPRECATED 冻结横幅）
# 详情见 `python/legacy/README.md` 与 `docs/refactor/31-stage11-sunset.md`（S11.2 / S11.3）。
# 该 Python 路径将在 S11.3（归档后 ≥2 个发布迭代）删除，历史保留在 git。