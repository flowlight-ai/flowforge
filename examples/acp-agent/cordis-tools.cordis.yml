# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@flowforge/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@flowforge/cordis-host-runner'
          - id: tool-cordis
            name: '@flowforge/tool-cordis'
