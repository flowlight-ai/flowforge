# 工程通用规范（强制）

> 本规范在 flowforge 项目执行。Git 工作流规范见 [git-workflow.md](git-workflow.md)。

## 1. 测试交付规范

> 测试不仅是"跑通"，更要"留痕"。测试人员交付物必须包含可追踪的缺陷清单，开发人员据此修复闭环。

### 1.1 测试三大交付物（缺一不可）

| 交付物 | 说明 | 位置 |
|--------|------|------|
| ① 测试用例 | 单元 / 集成 / E2E / 回归用例，遵循 T7（LLM 审核）与 T8（浏览器 DOM 验证）铁律 | `tests/`（单测 `tests/unit/`）|
| ② 测试报告 | 执行结果、通过率、T7/T8 覆盖情况 | `docs/test/` 下报告文件 |
| ③ **问题列表（必产出）** | 所有测试发现的缺陷提单，分级分类 + 计算 DI | **`docs/test/bugs.md`** |

- **问题列表为强制交付物**：凡测试发现的问题，必须录入 `docs/test/bugs.md`，禁止仅靠口头/对话反馈。
- 命名规范：主跟踪单固定为 **`bugs.md`**；禁止模糊命名（如 `问题列表.md`、`issues.txt`）。

### 1.2 问题列表格式规范

- **分级（Severity）**：S1 阻断 / S2 严重 / S3 一般 / S4 轻微。
- **DI 计算**：加权缺陷指数 DI = Σ 严重度权重（S1×10 + S2×5 + S3×2 + S4×1），仪表盘须随每次测试刷新。
- **工单字段**：ID、标题、严重度、分类、状态（Open / 修复中 / Closed）、文件:行号、现象、建议修复、发现日期、T7/T8 关联。

### 1.3 修复闭环

1. 开发人员依据 `docs/test/bugs.md` 逐条修复，提交信息引用工单 ID（如 `fix: 修复 P-03 CI 零测试 [sherlock]`）。
2. 修复后由测试人员回归验证，通过则关闭工单（状态置 `Closed`）。
3. 禁止"修复不提单、提单不关闭"——未关闭工单视为未完结。

## 2. 测试铁律 T1-T9

| 编号 | 铁律 | 说明 |
|------|------|------|
| **T1** | 禁止使用 Mock LLM | E2E/集成测试必须调用真实 LLM（经 OpenRoute） |
| **T2** | 禁止使用假数据 | 测试输入必须是真实场景数据，不得用 "test"/"hello" 等 |
| **T3** | 禁止跳过验证 | 必须有具体断言，不得 `status in ("completed","error")` |
| **T4** | 禁止 Mock 工具 | web_search/publish/fact_check 等必须真实调用 |
| **T5** | 未实现即 Bug | 发现代码未实现必须记录为 Bug 并修复 |
| **T6** | 必须采集指标 | E2E 测试必须用 MetricsCollector 采集完整指标 |
| **T7** | LLM 内容必须经 LLM 审核 | 凡 LLM 生成的内容（代码/文章/评论/文案等），必须再调用**不同模型**审核通过才算验证通过 |
| **T8** | Web 功能必须操控浏览器验证 DOM | 凡涉及网页操作的功能（发布/上架/部署等），必须操控浏览器查看 DOM 确认真实成功，并对 DOM 内容调用 LLM 审核质量 |
| **T9** | 运行时数据文件必须存放 data 目录 | 禁止污染代码目录 |

- 生成/发布类测试脚本必须提供 `--t7` / `--t8` 开关并默认开启，违规关闭直接判失败。

## 3. 编程红线（违反任一红线，该次修改全部回滚）

| # | 红线 |
|---|------|
| 1 | 禁止添加 CoT 检测/中文比例检测 |
| 2 | 质量分阈值默认 0.85（可在配置中覆盖，禁止硬编码其他值） |
| 3 | 禁止使用 Mock LLM（T1） |
| 4 | 禁止使用假数据（T2） |
| 5 | 禁止跳过验证（T3） |
| 6 | 禁止只看退出码不检查输出质量 |
| 7 | 禁止在修复问题时修改不相关代码 |
| 8 | 禁止删除已有测试用例 |
| 9 | 禁止用继承替代组合/插件 |
| 10 | 禁止在 flowforge 中写死业务领域代码（flowforge 只承载通用引擎） |
| 11 | 禁止硬编码提示词/路径/密钥/端口（密钥一律环境变量注入） |
| 12 | 禁止绕过 DI 容器直接实例化 |
| 13 | 禁止直接操作数据库（须经数据访问层） |
| 14 | 禁止不按 prompts.md 和 rules.md 执行 |
| 15 | 禁止偷工减料（发现未实现即 Bug，T5） |

## 4. 文档约束

1. **单文件 ≤ 50KB**：超限必须拆分。
2. **顶层索引文件不重复正文**：rules.md / prompts.md 顶层仅做索引，正文在拆分文件中。
3. **过程记录归档**：修改记录、版本变迁说明、修订日志不放在规范/模板正文中，归入 docs/archive/ 或 CHANGELOG。
4. **术语规范**：统一使用既定术语表，禁止自造说法。

## 5. AI 工具合规（强制）

> 所有 AI 编码工具（Trae / Claude Code / Cursor / Gemini / OpenCode / WorkBuddy / Qorder 等）在本仓库工作时必须遵守。

1. **必须使用 `./mgr` 命令**进行 Git 操作，禁止直接 `git push`。
2. **必须遵守平台感知原则**：在当前平台目录下开发，仅向当前平台提交 PR。
3. **跨平台同步使用 `./mgr merge-cross`**，禁止在日常开发中同时推送双端。
4. **必须按 git-workflow.md §3 格式撰写提交信息**，包含 type、scope、智能体署名。
5. **提交前必须运行 lint 和测试**，不引入新问题。
6. AI 工具配置文件位置：
   - `AGENTS.md`（仓库根目录，所有工具通用，**首要读取**）
   - `CLAUDE.md`（Claude Code）
   - `.cursorrules`（Cursor）
   - `GEMINI.md`（Gemini）
   - `.github/copilot-instructions.md`（GitHub Copilot）
7. 完整 Git 工作流规范见 [git-workflow.md](git-workflow.md)。
