# T8.3a-a1 档案提案列表端点 实施计划

> **执行者必读**：使用 executing-plans（③）行内执行本计划。步骤使用 checkbox（`- [ ]`）跟踪。
> **分解位置**：T8.3a 四子项目（端→根→代理→UI）中的第 1 项；后续 a2 组合根、a3 代理切分、a4 审批 UI 各自独立 spec→plan。

**目标**：为 `@flowforge/cats-routes` 的 profile-updates 路由组补上列表端点 `GET /api/profile-updates`（当前根路径会落到 `/:id` 正则之外直接 404），使审批 UI 有数据可拉。
**架构**：`ProfileUpdatePort` 增**可选** `list(query)`（既有实现方不受影响）→ router 在 `/:id` 正则**之前**拦截根路径 → 未实现 list 的端口返回 501；验证走 `@flowforge/host-cats-api` + 真实 `webServer` 端口 + 真实 fetch。
**技术栈**：TypeScript、zod（cats-routes 自带）、`@flowforge/cordis`、`@flowforge/host-webserver`、`@flowforge/host-cats-api`（PR#163 已交付）。
**规格**：`docs/process/specs/2026-09-08-web-t83a-approval-design.md`

## 全局约束

- `list` 必须为**可选**方法：既有 `IProfileUpdateProposalStore`（`packages/cats/stores/src/memory/profile-update-proposal-store.ts:61,65` 只有 `listPending`/`listByThread`）无通用 list，强制必填会破坏实现方
- 根路径分支必须写在 `/^\/api\/profile-updates\/([^/]+)(\/approve|\/reject)?$/` 正则匹配**之前**（`router.ts:173`）
- 身份解析沿用 `resolveUserId(request)`，`x-user-id` 缺失返回 401（不放宽）
- 分页用游标（`cursor`），不用 offset——与既有 `listForThread`/`listByUser` 的 limit 语义一致
- 真实 HTTP 验证（真实端口 + 真实 fetch），禁 mock（T1/T2/T3）
- 提交走 `./mgr commit` / `./mgr sync`，规范 `type(scope): 描述 [sherlock]`

---

### 任务 1：端口契约增补

**文件**：
- 修改：`packages/cats/routes/src/ports.ts`（`ProfileUpdatePort` 附近，约 44-54 行）

**接口**：
- 产出：`ProfileUpdateListQuery` / `ProfileUpdateListResult` 类型与可选 `list`

- [ ] **步骤 1：增补类型与可选方法**

在 `ProfileUpdatePort` 定义前插入：

```ts
/** 档案更新提案列表查询（审批 UI 的列表面）。 */
export interface ProfileUpdateListQuery {
  readonly status?: 'pending' | 'approved' | 'rejected' | undefined
  readonly createdBy?: string | undefined
  readonly limit?: number | undefined
  /** 上一页返回的游标；缺省从首页开始。 */
  readonly cursor?: string | undefined
}

/** 档案更新提案列表结果（游标分页）。 */
export interface ProfileUpdateListResult {
  readonly items: readonly Record<string, unknown>[]
  readonly nextCursor?: string
}
```

并将 `ProfileUpdatePort` 改为（新增可选 `list`，其余成员不变）：

```ts
export interface ProfileUpdatePort {
  get(proposalId: string): Promise<object | null> | object | null;
  claimForApproval(proposalId: string, approvedBy: string): unknown | Promise<unknown>;
  finalizeApproval(proposalId: string): unknown | Promise<unknown>;
  markRejected(
    proposalId: string,
    rejectedBy: string,
    rejectionReason?: string,
  ): unknown | Promise<unknown>;
  /** 可选：未实现时列表端点返回 501（既有存储仅有 listPending/listByThread）。 */
  list?(query: ProfileUpdateListQuery): Promise<ProfileUpdateListResult> | ProfileUpdateListResult;
}
```

- [ ] **步骤 2：导出类型测试确认**

```sh
node -e "const s=require('fs').readFileSync('packages/cats/routes/src/ports.ts','utf8');console.log(/ProfileUpdateListQuery/.test(s),/list\?\(query: ProfileUpdateListQuery\)/.test(s))"
```

测试通过标准：输出 `true true`；测试确认失败则核对插入内容后重跑。

---

### 任务 2：router 根路径分支

**文件**：
- 修改：`packages/cats/routes/src/router.ts`（在 `// ── profile-updates ──` 注释与 `profileMatch` 正则之间插入，约 172-174 行）

**接口**：
- 消费：任务 1 的 `ProfileUpdateListQuery` / `ProfileUpdateListResult`

- [ ] **步骤 1：插入根路径分支（必须在正则之前）**

```ts
    // ── profile-updates ──
    if (deps.profileUpdates !== undefined && pathname === '/api/profile-updates' && method === 'GET') {
      const userId = resolveUserId(request)
      if (!userId) return json(401, { error: 'Identity required' })
      if (deps.profileUpdates.list === undefined) {
        return json(501, { ok: false, error: 'list not supported by this port' })
      }
      const params = new URL(request.url).searchParams
      const statusParam = params.get('status')
      const status = statusParam === 'pending' || statusParam === 'approved' || statusParam === 'rejected'
        ? statusParam
        : undefined
      const createdBy = params.get('createdBy') ?? undefined
      const cursor = params.get('cursor') ?? undefined
      const limitParam = params.get('limit')
      const limit = limitParam === null ? undefined : Number(limitParam)
      const result = await deps.profileUpdates.list({
        ...(status === undefined ? {} : { status }),
        ...(createdBy === undefined ? {} : { createdBy }),
        ...(cursor === undefined ? {} : { cursor }),
        ...(limit === undefined || !Number.isFinite(limit) ? {} : { limit }),
      })
      return json(200, result as unknown as Json)
    }
```

- [ ] **步骤 2：确认插入位置在正则之前**

```sh
node -e "const s=require('fs').readFileSync('packages/cats/routes/src/router.ts','utf8');const root=s.indexOf(\"pathname === '/api/profile-updates'\");const re=s.indexOf('const profileMatch');console.log(root>-1, re>-1, root<re)"
```

测试通过标准：输出 `true true true`（根路径分支存在且位于正则之前）；测试确认失败则调整插入位置后重跑。

- [ ] **步骤 3：提交**

```sh
git add packages/cats/routes/src/ports.ts packages/cats/routes/src/router.ts
./mgr commit "feat(cats-routes): T8.3a-a1档案提案列表端点+可选list端口 [sherlock]"
```

---

### 任务 3：真实 HTTP 集成测试

**文件**：
- 新建：`packages/host/cats-api/tests/profile-updates-list.spec.ts`

**接口**：
- 消费：任务 1/2 的端口与路由；`mountCatsApi`（`packages/host/cats-api/src/index.ts`）

- [ ] **步骤 1：写集成测试（真实 Context + webServer 端口 + 真实 fetch，禁 mock）**

```ts
import { afterEach, describe, expect, it } from 'vitest'
import { Context } from '@flowforge/cordis'
import { WebServer } from '@flowforge/host-webserver'
import type { CatsRoutesDeps, ProfileUpdateListQuery } from '@flowforge/cats-routes'
import { mountCatsApi } from '../src/index.ts'

let ctx: Context | undefined

afterEach(async () => {
  await ctx?.fiber.dispose()
  ctx = undefined
})

const PROPOSALS = [
  { id: 'p1', status: 'pending', createdBy: 'u1' },
  { id: 'p2', status: 'approved', createdBy: 'u1' },
  { id: 'p3', status: 'pending', createdBy: 'u2' },
]

async function boot(deps: CatsRoutesDeps): Promise<string> {
  const root = new Context()
  await root.plugin(WebServer, { host: '127.0.0.1', port: 0 })
  mountCatsApi(root, { routes: deps })
  ctx = root
  return `http://127.0.0.1:${root.webServer.port}`
}

function portWithList(): CatsRoutesDeps {
  return {
    profileUpdates: {
      get: async id => PROPOSALS.find(p => p.id === id) ?? null,
      claimForApproval: async () => ({}),
      finalizeApproval: async id => ({ id, status: 'approved' }),
      markRejected: async id => ({ id, status: 'rejected' }),
      list: async (query: ProfileUpdateListQuery) => {
        const filtered = PROPOSALS.filter(p =>
          (query.status === undefined || p.status === query.status)
          && (query.createdBy === undefined || p.createdBy === query.createdBy))
        const start = query.cursor === undefined ? 0 : Number(query.cursor)
        const limit = query.limit ?? 50
        const page = filtered.slice(start, start + limit)
        const next = start + limit < filtered.length ? String(start + limit) : undefined
        return { items: page, ...(next === undefined ? {} : { nextCursor: next }) }
      },
    },
  }
}

describe('档案提案列表端点（真实 webServer）', () => {
  it('根路径返回 200 与全部提案（此前落 404）', async () => {
    const base = await boot(portWithList())
    const response = await fetch(`${base}/api/profile-updates`, { headers: { 'x-user-id': 'u1' } })
    expect(response.status).toBe(200)
    const body = await response.json() as { items: Array<{ id: string }> }
    expect(body.items.map(item => item.id)).toEqual(['p1', 'p2', 'p3'])
  })

  it('status 过滤与 limit 游标分页生效', async () => {
    const base = await boot(portWithList())
    const pending = await fetch(`${base}/api/profile-updates?status=pending`, { headers: { 'x-user-id': 'u1' } })
    const pendingBody = await pending.json() as { items: Array<{ id: string }> }
    expect(pendingBody.items.map(item => item.id)).toEqual(['p1', 'p3'])

    const firstPage = await fetch(`${base}/api/profile-updates?limit=2`, { headers: { 'x-user-id': 'u1' } })
    const firstBody = await firstPage.json() as { items: Array<{ id: string }>; nextCursor?: string }
    expect(firstBody.items).toHaveLength(2)
    expect(firstBody.nextCursor).toBe('2')
    const secondPage = await fetch(`${base}/api/profile-updates?limit=2&cursor=${firstBody.nextCursor ?? ''}`, { headers: { 'x-user-id': 'u1' } })
    const secondBody = await secondPage.json() as { items: Array<{ id: string }> }
    expect(secondBody.items.map(item => item.id)).toEqual(['p3'])
  })

  it('缺身份头 → 401；/:id 未知提案仍 404（回归保护）', async () => {
    const base = await boot(portWithList())
    const denied = await fetch(`${base}/api/profile-updates`)
    expect(denied.status).toBe(401)
    const missing = await fetch(`${base}/api/profile-updates/nope`, { headers: { 'x-user-id': 'u1' } })
    expect(missing.status).toBe(404)
  })

  it('端口未实现 list → 501 且不掩盖既有决策链路', async () => {
    const base = await boot({
      profileUpdates: {
        get: async id => PROPOSALS.find(p => p.id === id) ?? null,
        claimForApproval: async () => ({}),
        finalizeApproval: async id => ({ id, status: 'approved' }),
        markRejected: async id => ({ id, status: 'rejected' }),
      },
    })
    const unsupported = await fetch(`${base}/api/profile-updates`, { headers: { 'x-user-id': 'u1' } })
    expect(unsupported.status).toBe(501)
    const approved = await fetch(`${base}/api/profile-updates/p1/approve`, {
      method: 'POST',
      headers: { 'content-type': 'application/json', 'x-user-id': 'u1' },
      body: JSON.stringify({ decidedBy: 'u1' }),
    })
    expect(approved.status).toBe(200)
  })
})
```

- [ ] **步骤 2：运行测试确认**

```sh
pnpm vitest run packages/host/cats-api
```

测试通过标准：既有 7 + 新增 4 共 11 passed / 0 failed；测试确认失败则按断言定位（401/501/404 归属、游标计算）修正后重跑。

- [ ] **步骤 3：提交**

```sh
git add packages/host/cats-api/tests/profile-updates-list.spec.ts
./mgr commit "test(cats-api): T8.3a-a1列表端点真实HTTP集成测试(过滤/游标/401/501) [sherlock]"
```

---

### 任务 4：验证证据与 PR 收尾

**文件**：
- 无源码变更；产物为验证证据与 PR

- [ ] **步骤 1：登记证据并推进实例**

```sh
node packages/plugins/dev/bin/ff_dev.mjs evidence plugin-web-t83a --command "pnpm vitest run packages/host/cats-api" --exit 0 --summary "列表端点集成测试 4 + 既有 7 全绿（真实 webServer 端口与真实 fetch）"
node packages/plugins/dev/bin/ff_dev.mjs evidence plugin-web-t83a --command "pnpm lint" --exit 0 --summary "lint 0 errors"
node packages/plugins/dev/bin/ff_dev.mjs status plugin-web-t83a
```

测试通过标准：`status` 输出含 `designApproved=✓ planValidated=✓ verificationEvidence=✓` 且推进至 finish。

- [ ] **步骤 2：提交流程产物并建 PR**

```sh
git add docs/process/specs/2026-09-08-web-t83a-approval-design.md docs/process/plans/2026-09-08-web-t83a-a1.md docs/process/instances/plugin-web-t83a.json docs/process/verifications/plugin-web-t83a.md
./mgr commit "docs(process): T8.3a-a1流程产物-设计/计划/实例/验证证据 [sherlock]"
./mgr sync "feat(cats-routes): T8.3a-a1档案提案列表端点(审批UI数据面) [sherlock]" --body "T8.3a 第 1 子项目（实例 plugin-web-t83a，feature 工作流）：补 GET /api/profile-updates 列表端点——此前根路径不在 /:id 正则覆盖内会直接 404。ProfileUpdatePort 增可选 list(query)（既有存储只有 listPending/listByThread，强必填会破坏实现方），未实现时返回 501；路由分支置于正则之前，支持 status/createdBy/limit/cursor，身份沿用 x-user-id（缺则 401）。验证：真实 webServer 端口 + 真实 fetch 集成测试 4 项（列表/status 过滤+游标分页/401 与 404 回归/501 且不掩盖决策链路），既有 7 项不回归，lint 0 errors。后续：a2 组合根 web bundle、a3 next.config 代理切分、a4 审批面板档案提案 Tab。""
```

测试通过标准：`./mgr sync` 返回 Gitee PR 链接，PR 标题 ≤191 字符。

---

## 自审记录

- **规格覆盖**：设计文档"端点层（a1）"→ 任务 1（端口契约）、任务 2（路由分支）、任务 3（真实 HTTP 测试）、任务 4（证据+PR）。无遗漏。
- **占位符扫描**：无未决占位标记/"同任务 N"式懒引用；类型、路由分支、测试均给出全文。
- **类型一致性**：`ProfileUpdateListQuery`/`ProfileUpdateListResult`/可选 `list` 在设计文档与任务 1/2/3 一致；`mountCatsApi`、`WebServer`、`ctx.fiber.dispose` 取自已验证用法（PR#163）。
