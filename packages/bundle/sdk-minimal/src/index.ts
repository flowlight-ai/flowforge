/**
 * @flowforge/sdk-minimal — the standalone minimal SDK profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the
 * `flowforge.bundle.patch` manifest field and resolved by the profile
 * composer; this module carries no runtime API.
 * @module @flowforge/sdk-minimal
 */

export {}