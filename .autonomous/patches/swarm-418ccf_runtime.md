# 自主任务产出审阅

- **task_id**: swarm-418ccf5e4964
- **title**: 修复代码 TODO: flowforge\evolution\runtime.py
- **agent**: forgemind:sherlock
- **model**: openroute/doubao-web/chat
- **generated_at**: 2026-08-01T00:35:18.006124+00:00
- **source_file**: flowforge\evolution\runtime.py
- **required_capabilities**: code_generation

## 审阅指南

1. 检查产出的代码是否符合项目规范（CONTRIBUTING.md 15 条红线）
2. 检查是否引入循环依赖或违反分层架构
3. 通过审核后，将下方代码块内容应用到对应源文件
4. 应用后必须运行对应测试验证（铁律 T1-T8）

## 任务上下文

```
文件 flowforge\evolution\runtime.py 中发现 1 处 TODO/FIXME/NotImplementedError。请分析代码上下文并实现缺失的逻辑。
```

## LLM 产出内容

= {"wenxin": SelfDevDocLoop, "sherlock": SelfDevCodeLoop, "luban": SelfDevFrameworkLoop, "vangogh": SelfDevReviewLoop, "davinci": SelfDevTestLoop,} # 需要 approval_callback 的 forgekin（I8 不变量：framework 闭环） _FRAMEWORK_FORGEKIN_ID = "luban" # approval_mode 类型别名 ApprovalMode = Literal ["auto", "manual", "im"] # approval_callback 类型别名（与 self_dev_framework.ApprovalCallback 一致） ApprovalCallback = Callable [[DevPlan, DevTask], Awaitable [bool]] class SelfDevRuntime: """生产环境 SelfDev 五闭环运行时装配点（F046 §3.1 Phase 5 + F045 桥接）. 负责： - 加载 trae_bridge.yaml 创建 TraeLLMClient - 加载 5 个 forgekin YAML 配置 - 注入 approval_callback（基于 ApprovalHub + F047 IM 议事通道） - 创建 ForgeMindEngine + 5 个 SelfDev 闭环实例并注册 用法： runtime = SelfDevRuntime.bootstrap () result = await runtime.run_doc_loop ({"task_source":"scan"})""" def init(self, *, trae_client: TraeLLMClient, bridge_config: TraeBridgeConfig, protocol: TraeBridgeProtocol, engine: ForgeMindEngine, approval_hub: ApprovalHub, forgekin_configs: Dict [str, Dict [str, Any]], approval_mode: ApprovalMode = "manual", approval_timeout_seconds: int = 300, ) -> None: """初始化 SelfDevRuntime（由 bootstrap 调用，不直接实例化）. Args: trae_client: TraeLLMClient 实例（F045 桥接） bridge_config: TraeBridgeConfig 桥接配置 protocol: TraeBridgeProtocol 协议层 engine: ForgeMindEngine 实例（已注册 5 个 SelfDev 闭环） approval_hub: ApprovalHub 实例（CL-033） forgekin_configs: 5 个 forgekin 的配置字典 approval_mode: 审批模式（"auto"/"manual"/"im"） approval_timeout_seconds: 审批超时秒数（从 trae_bridge.yaml 读取）""" self._trae_client = trae_client self._bridge_config = bridge_config self._protocol = protocol self._engine = engine self._approval_hub = approval_hub self._forgekin_configs = forgekin_configs self._approval_mode: ApprovalMode = approval_mode self._approval_timeout_seconds: int = approval_timeout_seconds # request_id -> asyncio.Event（operator 决策后 set，唤醒等待中的 callback） self._pending_events: Dict [str, asyncio.Event] = {} # request_id -> approved（决策结果缓存，供 approval_callback 读取） self._decisions: Dict [str, bool] = {} self._logger = logger self._logger.info(f"SelfDevRuntime 初始化: approval_mode={approval_mode}," f"timeout={approval_timeout_seconds} s," f"forgekins={list (forgekin_configs.keys ())}" ) # ══════════════════════════════════════════════════════════════ # §1 bootstrap — 生产装配入口 # ══════════════════════════════════════════════════════════════ @classmethod def bootstrap ( cls, *, trae_bridge_yaml_path: str = _DEFAULT_TRAE_BRIDGE_YAML, approval_mode: ApprovalMode = "manual", project_root: Optional [str] = None, ) -> "SelfDevRuntime": """生产装配入口 — 加载配置 + 创建实例 + 注册闭环. Args: trae_bridge_yaml_path: trae_bridge.yaml 路径（默认 flowforge/config/trae_bridge.yaml） approval_mode: 审批模式： -"auto": 自动批准（仅 demo / 测试用，记录警告日志） -"manual": 通过 ApprovalHub，operator 调用 runtime.approve/reject 决策 -"im": 通过 F047 IM 议事通道推送（F047 完成后启用，当前降级为 manual） project_root: 项目根目录（None 用当前工作目录） Returns: SelfDevRuntime 实例（已注册 5 个 SelfDev 闭环） Raises: FileNotFoundError: trae_bridge.yaml 或 forgekin YAML 不存在 KeyError: forgekin 配置缺少必需字段（如 self_dev.loop_type）""" # 1. 加载 TraeBridgeConfig（红线 11：路径不硬编码，从 YAML 读取） bridge_config = TraeBridgeConfig.load_from_yaml (trae_bridge_yaml_path) logger.info(f"bootstrap 加载桥接配置: shared_dir={bridge_config.shared_dir}," f"default_timeout={bridge_config.default_timeout_seconds} s" ) # 2. 创建 TraeBridgeProtocol（DI 注入 bridge_config，红线 12） protocol = TraeBridgeProtocol (bridge_config) # 3. 创建 TraeLLMClient（DI 注入 protocol + bridge_config，红线 12） trae_client = TraeLLMClient ( bridge_config=bridge_config, protocol=protocol, ) # 4. 加载 5 个 forgekin YAML 配置（铁律 5：配置外置到 YAML） root = project_root or str (Path.cwd ()) forgekin_configs: Dict [str, Dict [str, Any]] = {} for forgekin_id in BUILTIN_FORGEKINS: try: cfg = load_forgekin_config (forgekin_id) except (KeyError, FileNotFoundError) as e: logger.error (f"bootstrap 加载 forgekin 失败: id={forgekin_id}, error={e}") raise # 注入 project_root（SelfDevLoopBase 要求 forgekin_config 含 project_root） cfg ["project_root"] = root forgekin_configs [forgekin_id] = cfg self_dev_cfg = cfg.get ("self_evolution", {}).get ("self_dev", {}) logger.info(f"bootstrap 加载 forgekin: id={forgekin_id}," f"loop_type={self_dev_cfg.get ('loop_type', '')}," f"stage={self_dev_cfg.get ('awakening_stage', '')}" ) # 5. 创建 ForgeMindEngine（治理层） engine = ForgeMindEngine () # 6. 创建 ApprovalHub（CL-033 跨 thread 统一审批中心） approval_hub = ApprovalHub () # 7. 创建 SelfDevRuntime 实例（提前创建以便注入 approval_callback 闭包） approval_timeout = bridge_config.default_timeout_seconds runtime = cls ( trae_client=trae_client, bridge_config=bridge_config, protocol=protocol, engine=engine, approval_hub=approval_hub, forgekin_configs=forgekin_configs, approval_mode=approval_mode, approval_timeout_seconds=approval_timeout, ) # 8. 为 luban 注入 approval_callback（I8 不变量：framework 闭环必须注入） approval_callback = runtime._make_approval_callback (_FRAMEWORK_FORGEKIN_ID) if _FRAMEWORK_FORGEKIN_ID in forgekin_configs: forgekin_configs [_FRAMEWORK_FORGEKIN_ID]["approval_callback"] = approval_callback logger.info(f"bootstrap 注入 approval_callback: forgekin={_FRAMEWORK_FORGEKIN_ID}," f"mode={approval_mode}" ) # 9. 实例化 5 个 SelfDev 闭环并注册到 engine（DI 注入，红线 12） for forgekin_id, loop_cls in _FORGEKIN_LOOP_CLASSES.items (): cfg = forgekin_configs [forgekin_id] self_dev_cfg = cfg.get ("self_evolution", {}).get ("self_dev", {}) loop_type = self_dev_cfg.get ("loop_type") awakening_stage = self_dev_cfg.get ("awakening_stage", "E3") if not loop_type: raise KeyError ( f"forgekin {forgekin_id} 配置缺少 self_evolution.self_dev.loop_type" ) loop_instance = loop_cls ( trae_client=trae_client, forgekin_config=cfg, evolution_engine=engine, awakening_stage=awakening_stage, ) engine.register_self_dev_loop (loop_instance) logger.info(f"bootstrap 注册 SelfDev 闭环: forgekin={forgekin_id}," f"loop_type={loop_type}, stage={awakening_stage}" ) logger.info(f"bootstrap 完成: approval_mode={approval_mode}," f"registered_loops={engine.list_self_dev_loops ()}" ) return runtime # ══════════════════════════════════════════════════════════════ # §2 approval_callback 工厂 # ══════════════════════════════════════════════════════════════ def _make_approval_callback (self, forgekin_id: str) -> ApprovalCallback: """创建 approval_callback（I8 不变量：framework 闭环必须注入）. 行为根据 approval_mode： -"auto": 记录警告日志，自动返回 True（仅 demo / 测试用） -"manual": 提交到 ApprovalHub，等待 operator 决策（通过 asyncio.Event） -"im": 通过 F047 IM 议事通道推送（F047 完成后启用，当前降级为 manual） Args: forgekin_id: 发起审批的 Forgekin ID Returns: async approval_callback (plan, task) -> bool""" runtime = self mode = self._approval_mode timeout_seconds = self._approval_timeout_seconds hub = self._approval_hub log = self._logger async def approval_callback (plan: DevPlan, task: DevTask) -> bool: """I8 approval 回调 — 提交审批请求并等待 operator 决策.""" request_id = f"approval-{uuid.uuid4 ().hex [:16]}" expires_at = datetime.now (timezone.utc) + timedelta (seconds=timeout_seconds) # 提取目标路径（优先 task.target_path，回退到 plan.steps [0].path） target_path = task.target_path if not target_path and plan.steps: first_step = plan.steps [0] if isinstance (first_step, dict): target_path = first_step.get ("path", "") request = ApprovalRequest ( request_id=request_id, forgekin_id=forgekin_id, thread_id=f"self_dev:{task.loop_type}", request_type="config_change", title=f"Framework 变更: {target_path or '(未指定)'}", description=( f" 任务: {task.description}\n"f" 预期效果: {plan.expected_effect}\n"f" 风险评估: {plan.risk_assessment}\n"f" 步骤数: {len (plan.steps)}"), payload={"plan_id": plan.plan_id,"task_id": task.task_id,"loop_type": task.loop_type,"steps": plan.steps,"target_path": target_path, }, expires_at=expires_at, priority="high", ) # auto 模式：自动批准（仅 demo / 测试用，记录警告日志） if mode =="auto": log.warning ( f"approval_callback auto 模式：自动批准 request_id={request_id} "f"(仅 demo / 测试用，生产环境必须用 manual/im)") hub.submit (request) hub.approve ( request_id=request_id, decided_by="auto-approver", comments="auto 模式自动批准 ", ) return True # im 模式：通过 F047 IM 议事通道推送（F047 完成前降级为 manual） if mode =="im": log.info(f"approval_callback im 模式：通过 F047 IM 议事通道推送" f"request_id={request_id}（F047 未完成，降级为 manual 等待）" ) # TODO: F047 完成后接入 IM 推送，当前降级为 manual 等待 # manual /im 降级：提交到 ApprovalHub，等待 operator 决策 hub.submit (request) # 创建 asyncio.Event 等待 operator 决策 event = asyncio.Event () runtime._pending_events [request_id] = event log.info(f"approval_callback 等待 operator 决策: request_id={request_id}," f"forgekin={forgekin_id}, target={target_path}," f"timeout={timeout_seconds} s" ) try: await asyncio.wait_for (event.wait (), timeout=timeout_seconds) except asyncio.TimeoutError: log.warning ( f"approval_callback 超时: request_id={request_id}," f"timeout={timeout_seconds} s（视为拒绝）" ) runtime._pending_events.pop (request_id, None) runtime._decisions.pop (request_id, None) return False # 获取决策结果（由 approve/reject 写入） approved = runtime._decisions.pop (request_id, False) runtime._pending_events.pop (request_id, None) log.info(f"approval_callback 收到决策: request_id={request_id}," f"approved={approved}" ) return approved return approval_callback # ══════════════════════════════════════════════════════════════ # §3 五个 run_xxx_loop 方法 — 委托给 engine.run_self_dev_loop # ══════════════════════════════════════════════════════════════ async def run_doc_loop (self, context: dict) -> dict: """运行文档闭环（wenxin / 文心，E3）. 委托给 engine.run_self_dev_loop ("doc", context). Args: context: 循环上下文（含 task_source /scan_patterns 等） Returns: SelfDevLoopBase.run_once 返回的循环执行结果""" self._logger.info("run_doc_loop 启动") return await self._engine.run_self_dev_loop ("doc", context) async def run_code_loop (self, context: dict) -> dict: """运行代码闭环（sherlock / 夏洛克，E4）. 委托给 engine.run_self_dev_loop ("code", context). Args: context: 循环上下文（含 task_source /target_files 等） Returns: SelfDevLoopBase.run_once 返回的循环执行结果""" self._logger.info("run_code_loop 启动") return await self._engine.run_self_dev_loop ("code", context) async def run_framework_loop (self, context: dict) -> dict: """运行框架闭环（luban / 鲁班，E5，含 I8 approval）. 委托给 engine.run_self_dev_loop ("framework", context). 所有 Act 操作会触发 approval_callback 等待 operator 显式批准（I8 不变量）. Args: context: 循环上下文（含 task_source /force_targets/check_architecture 等） Returns: SelfDevLoopBase.run_once 返回的循环执行结果""" self._logger.info("run_framework_loop 启动（I8 approval 已注入）") return await self._engine.run_self_dev_loop ("framework", context) async def run_review_loop (self, context: dict) -> dict: """运行审查闭环（vangogh / 梵高，E3）. 委托给 engine.run_self_dev_loop ("review", context). Args: context: 循环上下文（含 task_source /review_targets 等） Returns: SelfDevLoopBase.run_once 返回的循环执行结果""" self._logger.info("run_review_loop 启动") return await self._engine.run_self_dev_loop ("review", context) async def run_test_loop (self, context: dict) -> dict: """运行测试闭环（davinci / 达芬奇，E3）. 委托给 engine.run_self_dev_loop ("test", context). Args: context: 循环上下文（含 task_source /target_files 等） Returns: SelfDevLoopBase.run_once 返回的循环执行结果""" self._logger.info("run_test_loop 启动") return await self._engine.run_self_dev_loop ("test", context) # ══════════════════════════════════════════════════════════════ # §4 operator 接口 # ══════════════════════════════════════════════════════════════ def approve (self, request_id: str, comments: str = "") -> bool:"""operator 批准审批请求。唤醒等待中的 approval_callback，使其返回 True. Args: request_id: ApprovalRequest.request_id comments: 批准备注 Returns: True 表示决策已记录，False 表示 request_id 不存在 / 已决策 / 已过期 """ok, reason = self._approval_hub.approve ( request_id=request_id, decided_by="operator", comments=comments, ) if not ok: self._logger.warning ( f"approve 失败: request_id={request_id}, reason={reason}" ) return False self._decisions [request_id] = True event = self._pending_events.get (request_id) if event is not None: event.set () self._logger.info(f"approve 成功: request_id={request_id}") return True def reject (self, request_id: str, comments: str = "") -> bool:"""operator 拒绝审批请求。唤醒等待中的 approval_callback，使其返回 False. Args: request_id: ApprovalRequest.request_id comments: 拒绝备注 Returns: True 表示决策已记录，False 表示 request_id 不存在 / 已决策 / 已过期 """ok, reason = self._approval_hub.reject ( request_id=request_id, decided_by="operator", comments=comments, ) if not ok: self._logger.warning ( f"reject 失败: request_id={request_id}, reason={reason}" ) return False self._decisions [request_id] = False event = self._pending_events.get (request_id) if event is not None: event.set () self._logger.info(f"reject 成功: request_id={request_id}") return True def list_pending_approvals (self) -> List [Dict [str, Any]]: """列出所有待审批请求（operator 查看用）. Returns: 待审批请求列表，每项含 request_id/forgekin_id/title/description/priority/created_at/expires_at/payload""" pending = self._approval_hub.list_pending () return [ { "request_id": r.request_id, "forgekin_id": r.forgekin_id, "thread_id": r.thread_id, "request_type": r.request_type, "title": r.title, "description": r.description, "priority": r.priority, "created_at": r.created_at.isoformat (), "expires_at": r.expires_at.isoformat (), "payload": r.payload, } for r in pending ] def get_stats (self) -> Dict [str, Any]: """获取运行时统计信息. Returns: {"approval_stats": {pending, approved, rejected, deferred, expired},"registered_loops": {loop_type: min_awakening_stage},"pending_events_count": int,"approval_mode": str,"approval_timeout_seconds": int, }""" return { "approval_stats": self._approval_hub.get_stats (), "registered_loops": self._engine.list_self_dev_loops (), "pending_events_count": len (self._pending_events), "approval_mode": self._approval_mode, "approval_timeout_seconds": self._approval_timeout_seconds, } # ══════════════════════════════════════════════════════════════ # §5 资源管理 # ══════════════════════════════════════════════════════════════ async def shutdown (self) -> None: """清理资源 — 关闭 watcher、归档 pending 请求、唤醒等待中的 approval_callback. 唤醒等待中的 approval_callback 时视为拒绝（approved=False）. 调用后 runtime 不再可用，应重新 bootstrap 创建新实例.""" self._logger.info("shutdown 开始") # 1. 停止 watcher（如果由 protocol 内部拥有） try: await self._protocol.stop_watcher () except Exception as e: self._logger.warning (f"shutdown 停止 watcher 异常: {e}") # 2. 唤醒所有等待中的 approval_callback（视为拒绝） for request_id, event in list (self._pending_events.items ()): self._decisions [request_id] = False event.set () pending_count = len (self._pending_events) self._pending_events.clear () if pending_count > 0: self._logger.info(f"shutdown 唤醒 {pending_count} 个等待中的 approval_callback（视为拒绝）" ) # 3. 清理 ApprovalHub 过期请求 try: purged = self._approval_hub.purge_expired () if purged > 0: self._logger.info(f"shutdown 清理过期审批请求: count={purged}") except Exception as e: self._logger.warning (f"shutdown 清理 ApprovalHub 异常: {e}") self._logger.info("shutdown 完成") all = ["SelfDevRuntime"] --- 文件结束 --- 请在上述真实代码基础上，修复其中的 TODO/FIXME/NotImplementedError，输出完整的修改后文件内容。禁止生成假设性或示例性代码。 【重要】以上是项目真实文件内容，请基于实际代码和项目结构生成具体的、可执行的成果。禁止生成假设性代码或示例代码 —— 必须针对真实文件进行修改或补充。产出格式： - 文档任务：直接输出 Markdown 文档内容 - 代码任务：输出完整的修改后代码（带文件路径标注） - 测试任务：输出完整的测试代码（带文件路径标注）